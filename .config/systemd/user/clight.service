Description=Control various screen related properties (gamma, dpms, backlight)
Requires=clightd.service

[Service]
Type=simple
ExecStart=/usr/bin/clight
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
