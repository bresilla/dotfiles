super + alt + d
    /home/bresilla/.config/rofi/menu/data
super + alt + a
    /home/bresilla/.config/rofi/menu/apps
super + alt + b
    /home/bresilla/.config/rofi/menu/BROWSE
super + alt + p
    /home/bresilla/.config/rofi/menu/power
super + alt + c
    /home/bresilla/.config/rofi/menu/CONFIG
super + alt + s
    /home/bresilla/.config/rofi/menu/screens

super + alt + space; {space, Return}
    {kitty --class=kittyled, alacritty -e tmux}

{Insert, @Insert}
    /home/bresilla/.config/sxhkd/scripts/eww {--open, --close}
super + Insert
    /home/bresilla/.config/sxhkd/scripts/eww --open

Print; {Print,Insert}
    /home/bresilla/dots/.func/wm/sshoot {--flame, --maim}
XF86Search; {XF86Search, c}
    {pkill slop; slop -r boxzoom, /home/bresilla/dots/.func/wm/colorpicker}


{super,alt} + Escape
    bspc node -{c,k}
{super, alt, mode_switch} + Delete
    {lule create -- set, lule create -- set, /home/bresilla/.startup}

alt + space
    /home/bresilla/dots/.func/wm/scratch --term=kitty --class=MAIN --width=3000 --height=1050 -m -n
mode_switch + space
    /home/bresilla/dots/.func/wm/scratch --term=kitty --class=NOTE --width=3500 --height=1900 -m -n


super + alt + {Left,Right}
    bspc desktop -f {prev,next}.local
super + alt + {Down,Up}
    bspc monitor -f {prev,next}
{super,alt} + {Left,Down,Up,Right}
    /home/bresilla/.config/bspwm/scripts/{focus,move} {west,south,north,east}
{super,alt} + @button{6,5,4,7}
    /home/bresilla/.config/bspwm/scripts/{focus,move} {west,south,north,east}
{_, ctrl, shift} + {super+alt} + @button{6,5,4,7}
    /home/bresilla/.config/bspwm/scripts/resize {b,s,s} {Left,Down,Up,Right}
{super+alt} + shift + {Left,Right}
    bspc node -d {prev,next} --follow
{super+alt} + shift + {Down,Up}
    bspc node -m {prev,next} --follow

super + alt + Return; {Print,Insert,Delete,XF86MonBrightnessUp}
    bspc node {-t floating,-t fullscreen,-t tiled,-g sticky}

super + {w,f,t,s}
    bspc node {-t floating,-t fullscreen,-t tiled,-g sticky}

super + @p
    /home/bresilla/dots/.func/wm/youtube_play

XF86Audio{Prev,Next,Play}
    playerctl {previous,next,play-pause}
XF86Audio{Raise,Lower}Volume
    pulsemixer --change-volume {+,-}2; ([[ -z $(xdotool search -class volume) ]] && /env/cargo/bin/eww open volume && sleep 3 && /env/cargo/bin/eww close volume)&
XF86AudioMute
    pulsemixer --toggle-mute; ([[ -z $(xdotool search -class volume) ]] && /env/cargo/bin/eww open volume && sleep 3 && /env/cargo/bin/eww close volume)&
XF86MonBrightness{Up,Down}
    light -{A,U} 2; ([[ -z $(xdotool search -class light) ]] && /env/cargo/bin/eww open light && sleep 3 && /env/cargo/bin/eww close light)&
super + XF86MonBrightness{Up,Down}
    pulsemixer --change-volume {+,-}2; ([[ -z $(xdotool search -class volume) ]] && /env/cargo/bin/eww open volume && sleep 3 && /env/cargo/bin/eww close volume)&


ctrl + super + alt + {space,Left,Right,Return}
    /home/bresilla/.config/sxhkd/scripts/mpv {cycle,backwards,forewards,hide}
alt+super + XF86AudioPlay
    play ~/.config/dunst/scripts/bleep.wav; systemctl --user restart spotifyd
alt+super + XF86AudioMute
    pactl set-sink-port 0 analog-output-headphones
