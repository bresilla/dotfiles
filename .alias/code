alias one="nix-shell --command zsh"
alias six="nix-shell --command zsh support/shell.nix"

alias git='hub'
alias doc='docker'
alias docker='supdock'

alias dev='devenv'
alias pro='proji'

alias lines='tokei'

alias cdp='cd $(pro ls | head -n-1 | tail -n+4 | fzy | cut -d "|" -f4)'

alias search="rg"
